import { Repository } from 'typeorm';
import { BusinessOwner, BusinessAddress, BusinessMedia, BusinessService as BusinessServiceEntity, Service, ServiceCategory, Staff, StaffService, Booking, StaffWorkingHours, BusinessOperatingHours, ServicePackage, ServicePackageItem, Customer, CustomerFavorite, Review } from '../database/entities';
import { DistanceCalculatorService } from '../common/services/distance-calculator.service';
import { BusinessQueryDto, BusinessResponseDto, BusinessDetailResponseDto, BookedSlotsResponseDto, AvailableSlotsQueryDto, AvailableSlotsResponseDto } from './dto';
export declare class BusinessService {
    private readonly businessOwnerRepository;
    private readonly businessAddressRepository;
    private readonly businessMediaRepository;
    private readonly businessServiceRepository;
    private readonly serviceRepository;
    private readonly serviceCategoryRepository;
    private readonly staffRepository;
    private readonly staffServiceRepository;
    private readonly bookingRepository;
    private readonly staffWorkingHoursRepository;
    private readonly businessOperatingHoursRepository;
    private readonly servicePackageRepository;
    private readonly servicePackageItemRepository;
    private readonly customerRepository;
    private readonly customerFavoriteRepository;
    private readonly reviewRepository;
    private readonly distanceCalculatorService;
    constructor(businessOwnerRepository: Repository<BusinessOwner>, businessAddressRepository: Repository<BusinessAddress>, businessMediaRepository: Repository<BusinessMedia>, businessServiceRepository: Repository<BusinessServiceEntity>, serviceRepository: Repository<Service>, serviceCategoryRepository: Repository<ServiceCategory>, staffRepository: Repository<Staff>, staffServiceRepository: Repository<StaffService>, bookingRepository: Repository<Booking>, staffWorkingHoursRepository: Repository<StaffWorkingHours>, businessOperatingHoursRepository: Repository<BusinessOperatingHours>, servicePackageRepository: Repository<ServicePackage>, servicePackageItemRepository: Repository<ServicePackageItem>, customerRepository: Repository<Customer>, customerFavoriteRepository: Repository<CustomerFavorite>, reviewRepository: Repository<Review>, distanceCalculatorService: DistanceCalculatorService);
    getBusinesses(query: BusinessQueryDto, userId?: string, customerId?: string): Promise<BusinessResponseDto>;
    getBusinessDetail(shopId: string, userId?: string, customerId?: string): Promise<BusinessDetailResponseDto>;
    getBookedSlots(shopId: string, date: string, staffId?: string): Promise<BookedSlotsResponseDto>;
    private validateLocationParams;
    private buildBaseQuery;
    private applyCategoryFilter;
    private applyAvailabilityFilter;
    private applyPriceFilter;
    private applyGenderFilter;
    private applyOperatingYearsFilter;
    private applySearchFilter;
    private applyLocationFilter;
    private applySorting;
    private parseSortString;
    private applyPagination;
    private transformToBusinessItems;
    private sortBusinessesByDistance;
    private buildMetadata;
    getAvailableSlots(shopId: string, query: AvailableSlotsQueryDto): Promise<AvailableSlotsResponseDto>;
    private generateTimeSlots;
    private isTimeOverlapping;
    private checkFavoritesForBusinesses;
    private calculateBusinessRating;
    private calculateBulkBusinessRatings;
}
